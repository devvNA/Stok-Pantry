import 'package:flutter/material.dart';

class AppColors {
  // Warna hijau segar untuk elemen utama
  static const Color freshgreen = Color(0xFF4CAF50);

  // Warna hijau muda untuk latar belakang ringan kolom
  static const Color lightgreen = Color(0xFFC8E6C9);

  // Warna hijau gelap untuk elemen yang membutuhkan penekanan
  static const Color darkgreen = Color(0xFF388E3C);

  // Warna oranye untuk peringatan dan call-to-action
  static const Color orange = Color(0xFFFF5722);

  // Warna hitam standar untuk teks utama
  static const Color black = Color(0xFF212121);

  // Warna hitam yang lebih dalam untuk hintext
  static const Color deepblack = Color(0xFF3D3D3D);

  // Warna putih murni untuk teks elemen
  static const Color white = Color(0xFFFFFFFF);

  // Warna putih keabu-abuan untuk latar belakang halaman
  static const Color deepwhite = Color(0xFFF3F3F3);
}
